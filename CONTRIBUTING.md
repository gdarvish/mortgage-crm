# תרומה לקוד — mortgage-crm

## הכלל החשוב ביותר: טסט רגרסיה לכל תיקון באג

**כל PR שמתקן באג חייב לכלול טסט שנכשל לפני התיקון ועובר אחריו.**

זה לא פורמליות. ביקורת הפונקציונליות של המערכת מצאה 35 באגים, וכולם עברו
בשקט את 78 הטסטים שהיו קיימים — כי הטסטים כיסו את החישובים, והבאגים היו
בשכבות אחרות: כפתור בלי `onClick`, `where` חסר בשאילתה, אינדקס שלא הוגדר,
מיפוי הפוך בין שני שדות. טסט שנכתב אחרי התיקון ולא נבדק מול הבאג המקורי
לא מוכיח דבר.

**איך לוודא שהטסט באמת תופס את הבאג:** לפני שפותחים PR, החזירו זמנית את
הקוד השגוי (`git stash` על התיקון בלבד), הריצו את הטסט, וודאו שהוא נכשל.
רק אז החזירו את התיקון. אם הטסט עובר גם עם הבאג — הוא בודק את הדבר הלא נכון.

---

## שכבות הטסטים ומה שייך לכל אחת

| שכבה | איפה | מה נבדק | מתי להשתמש |
|---|---|---|---|
| יחידה | `src/**/*.test.ts` | פונקציות טהורות — חישובים, ולידציות, גזירות | לוגיקה עסקית ומתמטית |
| פרויקט | `tests/unit/*.test.ts` | אינדקסים, שומרי גישה ב-Cloud Functions | כללים שחלים על הקוד כולו |
| רכיבים | `tests/unit/*.test.tsx` | מסלולים קריטיים ב-UI | חיווט בין UI לשירותים |
| חוקים | `tests/emulator/firestore-rules.test.ts` | בעלות, בידוד בין יועצים | כל שינוי ב-`firestore.rules` |
| שירותים | `tests/emulator/services*.test.ts` | CRUD ושאילתות מול Firestore אמיתי | כל שינוי בשכבת השירותים |

**כלל אצבע לבחירת שכבה:** אם הבאג היה מתגלה בלי Firestore — טסט יחידה.
אם הוא נובע משאילתה, מחוקי גישה או מאינדקס — טסט אמולטור. אם הוא בחיווט
בין כפתור לשירות — טסט רכיב.

---

## הרצה

```bash
npm test            # טסטי יחידה, פרויקט ורכיבים — מהיר, ללא תלויות
npm run test:rules  # חוקי Firestore מול אמולטור
npm run test:services  # שכבת השירותים מול אמולטור
npm run test:all    # הכל — זה מה שרץ ב-CI
```

`test:rules` ו-`test:services` מפעילים את האמולטור בעצמם דרך
`firebase emulators:exec`, ולכן דורשים Java 17+ מותקן. אין צורך להפעיל
אמולטור ידנית.

לפני כל PR:

```bash
npx tsc -b && npm run lint && npm run test:all
```

וגם, לשינויים ב-Cloud Functions:

```bash
cd functions && npx tsc --noEmit
```

### מדידת כיסוי

`npx vitest run --coverage` דורש התקנה של `@vitest/coverage-v8` בגרסה
התואמת ל-vitest שבפרויקט. הוא אינו תלוּת קבועה כי גרף ה-peers שלו מתנגש
כרגע עם גרסת vitest הנוכחית.

---

## כללים ספציפיים לפרויקט הזה

### שאילתות Firestore

**חוקי Firestore אינם פילטרים.** שאילתת אוסף שאינה מצמצמת ל-`user_id` של
המשתמש המחובר נדחית **כולה** ב-`permission-denied` — לא מחזירה תוצאות
חלקיות. כל `getDocs` על אוסף חייב `where('user_id', '==', uid)`.

כל שאילתה שמשלבת פילטר עם `orderBy` על שדה אחר דורשת אינדקס קומפוזיטי.
האמולטור **מגיש כל שאילתה** בלי קשר לאינדקסים, ולכן אינדקס חסר לא נתפס
שם — הוא מתגלה רק מול Firestore האמיתי. לכן כל שאילתה כזו חייבת להירשם
ב-`tests/unit/firestore-indexes.test.ts` וב-`firestore.indexes.json`.

### Cloud Functions

Cloud Function רצה עם ה-Admin SDK ו**עוקפת את `firestore.rules` לחלוטין**.
אין קו הגנה שני. כל `onCall` חייב לקרוא ל-`requireAuth` או `requireAdmin`
מ-`functions/src/guards.ts`, וכל פונקציה שקוראת מסמך לפי מזהה שהגיע
מהקורא חייבת `requireOwnedDoc`. `tests/unit/function-guards.test.ts` אוכף
את זה — פונקציה חדשה בלי שומר תיפיל את ה-CI.

### פרמטרים רגולטוריים

מגבלות בנק ישראל (LTV, שליש קבוע, תקרת פריים, תקופה מקסימלית, ספי DTI,
גיל בתום תקופה, הנחות פירעון מוקדם) **אינן קבועות בקוד**. הן מתפרסמות
לאוסף `regulatory_params` דרך `updateRegulatoryParams`, ולכל רשומה
`effective_from` — כך שתיק נשפט לפי הכללים שהיו בתוקף בזמנו.

אין להוסיף מספר רגולטורי חדש כקבוע. מוסיפים שדה ל-`RegulatoryParams`,
ל-`FALLBACK_REGULATORY_PARAMS` (שמתועד במפורש כטעון אימות), ולולידציה
ב-`updateRegulatoryParams`.

### חישוב נתוני תיק

**אין למסך לחשב DTI, LTV או compliance בעצמו.** כל נתון גזור של תיק מגיע
מ-`useCaseSnapshot`, וכל שיפוט של תמהיל עובר דרך `evaluateMix`. זו הסיבה
שהמערכת יכלה להציג שני מספרים שונים לאותו נתון בשני טאבים.

בדיקה: `grep -rn "checkCompliance(" src/pages src/components` צריך לחזור ריק.

### מסמכי לקוח

אין לשמור `file_url` קבוע על מסמך. `getDownloadURL` מייצר קישור עם טוקן
שעוקף את `storage.rules` לצמיתות. גישה למסמך היא תמיד דרך
`documentService.getUrl`, שמייצר signed URL קצר-מועד אחרי בדיקת בעלות.

### פונקציות טהורות

לוגיקה שאפשר לבדוק בלי Firebase שייכת ל-`src/utils/`, לא לתוך שירות.
שירות שמייבא את `@/lib/firebase` אינו ניתן לייבוא בטסט יחידה. אם צריך
לבדוק פונקציה שיושבת בשירות — מוציאים אותה ל-`utils` ומייצאים מחדש
מהשירות לשם נוחות הקוראים.
